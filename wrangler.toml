# Rate limiting for /api/contact is handled at the Cloudflare dashboard level,
# not here. See the "Rate Limiting" section in README.md for the recommended
# rules (Security → Rate limiting rules, 10 req/min/IP, action Block).
# The per-isolate throttle in functions/api/contact.ts is defense-in-depth only.

name = "personalportfolio"
compatibility_date = "2024-12-01"
pages_build_output_dir = "dist"
