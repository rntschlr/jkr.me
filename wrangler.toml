# Rate limiting for /api/contact is enforced ONLY at the Cloudflare dashboard
# level, not in code. See the "Rate Limiting" section in README.md for the
# recommended rules (Security → Rate limiting rules, 10 req/min/IP, action Block).
# functions/api/contact.ts has no application-level throttle — the origin
# allowlist and field validation there are the only in-code gates.

name = "personalportfolio"
compatibility_date = "2024-12-01"
pages_build_output_dir = "dist"
